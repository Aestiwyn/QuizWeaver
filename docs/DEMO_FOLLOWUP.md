# 教师助手 Demo 后续事项

记录日期：2026-09-06。当前分支：teacher-copilot。

本文件用于跟踪教师助手 Demo 的后续事项。已完成项目按实际实现和验证结果更新；未明确处理的事项继续保持待办。

## 本轮确认的行为与待讨论方案

- [x] DEMO-01：首页“生成测验”和“记录课程”不再使用 `classes[0].id`。两个入口先进入只允许 `generate-quiz`、`log-lesson` 两种目标的班级选择流程，再进入 URL 锁定的生成或记录页面；班级详情中的按钮仍直接使用当前班级。表单不提供班级切换控件，后端按 URL 中的 `class_id` 校验和保存。
- [x] DEMO-02：`Legacy Class (Pre-Platform Expansion)` 保留为兼容旧数据的默认班级，迁移逻辑、原始名称、已有测验和归属均未修改。所有页面通过展示层过滤器将其显示为 `Default Class (Legacy Data)`；其他班级名称原样显示。
- [x] DEMO-03：界面默认语言已改为 `zh-CN`，未新增语言切换或国际化框架。教师核心流程的导航、班级、课程记录、测验生成、测验详情、题库、动态提示与导出说明改为简体中文；产品名、模型名、API 和课程标准代码保留原文。

## 上轮代码检查发现的问题（按实际进展更新）

- [x] DEMO-04：服务端读取并严格校验 `lesson_date`（YYYY-MM-DD），缺失时默认当天；无效日期返回表单错误且不创建记录。错误后保留日期、正文、主题和备注。列表、详情、刷新均显示保存日期；已验证自定义闰日及无效日期。
- [x] DEMO-05：保留并接通 PDF/DOCX 上传，复用已有 PyMuPDF、python-docx 依赖；正文、文件、两者同时提供均可保存。验证扩展名、实际内容、10 MB 文件上限和提取结果；拒绝空文件、伪装格式、损坏文件、加密 PDF 和无文字文件。无可提取文字时提示“未检测到可提取文本，请粘贴课程内容或上传可选择文字的文件”。解析失败即使有正文也返回错误，不自动降级为纯文本；未实现 OCR。
- [x] DEMO-06：服务端校验班级存在；空白正文且无有效文件时拒绝保存。验证失败不写持久文件；数据库提交失败时回滚课程/知识记录并删除本次上传。表单保留文字字段，并明确提示浏览器不能恢复文件输入、需要重新选择。
- [x] DEMO-07：当前 Web 生成流程提供“使用已记录课程”和“输入本次内容”两种明确来源。已记录课程只允许选择 URL 当前班级的一条记录；本次输入不要求先记录课程。两种方式均关闭最近 14 天课程和已知知识的自动加载，Generator 与 Critic 使用同一份选定内容。通用生成接口继续默认支持课程历史，CLI 和其他调用方行为保持兼容。
- [x] DEMO-08：生成页已移除按固定时间推进的“准备、生成、检查”等动画。提交后只显示“正在生成并检查题目，请稍候”；成功时直接进入测验详情，失败时显示后端安全的真实错误并可返回保留输入的表单重试。
- [x] DEMO-09：详情页依据 `metrics.ai_review_status` 和 `metrics.ai_review_reason` 显示检查通过、Critic 拒绝、重试上限、题目不足、Critic 异常或未知旧数据，不再把所有未通过结果写成达到重试上限。Critic 异常留下的结构有效草稿明确标记为检查失败或未完成。
- [x] DEMO-10：已增加独立的教师确认状态、时间和可选确认用户。新测验及旧测验默认待教师审核；确认使用 POST 和 CSRF；题目编辑、删除、重新生成及现有图片内容修改会使已确认测验转为修改后需重新确认，标题修改不会使其失效。未确认测验仍可导出，但页面会提示并要求教师确认继续。

## 后续产品建议（不等同于已确认缺陷）

- [ ] DEMO-11：生成题量默认值已改为 5；全部题型、默认勾选状态、课程标准、模型和难度交互保持现状。认知框架已从普通用户页面隐藏，未移入高级设置；其他低频配置是否需要高级设置仍待讨论。
- [ ] DEMO-12：配置文件默认使用 mock。正式演示前验证一次真实模型的生成、编辑与导出；当前不能据此认定真实供应商未配置。
- [ ] DEMO-13：核心流程稳定后再考虑每题原文定位，以及固定样本上的正确性、原文支持率、修改量和成本评测。

## 本次课程流程修复（2026-09-06）

- [x] 保存后以 303 跳转刚创建的课程详情；展示班级、真实课程日期、手动正文、主题、备注、原始文件名和提取文字摘要（前 1000 字符，可展开全文）。无附件的旧记录正常显示。
- [x] 原始文件使用 UUID 内部文件名独立保存；下载要求登录，并按 URL 中的班级和课程 ID 联合查询。拒绝不存在课程、错误班级、路径穿越及越界存储路径。下载使用附件响应和原始文件名的安全 basename；数据库保留提交的完整原始文件名。单课程删除也校验班级并删除对应原文件。
- [x] 首页先选班级，班级详情直达当前班级；记录、详情和由详情进入的生成页均不提供班级切换。
- [x] SQLite 迁移使用原有 SQL runner，新增 014/015/016 三个单字段迁移：`lesson_logs.original_filename`、`stored_filename`、`extracted_text`，全部可为空。分文件允许原 runner 在部分升级后重试；已验证旧记录保留、重复运行以及已添加一/两个字段后的继续升级。

存储默认位置：`D:\demo\QuizWeaver\uploads\lessons\<UUID>.pdf` 或 `.docx`，不在静态资源目录中；可通过现有配置中的 `paths.lesson_upload_dir` 指定目录。备份需同时包含数据库和课程上传目录。数据库保存手动正文与完整提取文字，只有内部 basename 存入 `stored_filename`，请求不能指定磁盘路径。

上传控件注明单文件 10 MB 上限，浏览器在提交前拦截超大文件并保留当前表单；服务端独立校验文件大小。课程端点额外设置 11 MB 总请求上限（包含表单和 multipart 开销），其余端点保留原 5 MB 限制。绕过浏览器并发送超过 11 MB 的请求返回 413，提示使用浏览器返回按钮恢复表单并重新选择文件；此时服务端不会读取或恢复请求中的文字字段。DOCX 解压处理上限为 50 MB。

本次修改文件：

- 数据与校验：`src/database.py`、`src/lesson_tracker.py`、`src/lesson_files.py`、`src/migrations.py`。
- Web：`src/web/app.py`、`src/web/blueprints/classes.py`、`src/web/blueprints/quizzes.py`。
- 页面：`templates/lessons/new.html`、`templates/lessons/list.html`、新增 `templates/lessons/detail.html`、`templates/classes/detail.html`、`templates/quizzes/generate.html`。
- 迁移：`migrations/014_lesson_original_filename.sql`、`015_lesson_stored_filename.sql`、`016_lesson_extracted_text.sql`。
- 测试：新增 `tests/test_lesson_workflow.py`、`tests/test_lesson_file_migrations.py`；本跟进文档。

验证结果：

- 课程、班级、班级选择、基础 Web、首页、测验 Web 和首页改版测试：309 项通过。
- 新课程流程测试 41 项、新字段迁移测试 3 项；另有安全、课程价值说明、多班级端到端及测验生成测试 55 项通过。上述相关测试合计 408 项（去重），均使用 Mock 或本地解析，无付费模型调用。
- 额外运行既有迁移测试 27 项：24 项通过、3 项失败。失败为 `test_migration_not_needed_after_full_run`、`test_run_migrations_idempotent` 和 `test_empty_db_needs_no_migration_when_orm_creates_tables`；用 `git show HEAD:src/migrations.py` 加载修改前的迁移模块后同样复现这 3 项失败，未扩大本任务修改范围。新增课程迁移用真实旧版 SQL schema 单独验证通过。
- Edge 无头浏览器开启真实登录和 CSRF，完成 12 项检查：首页选班级、班级锁定、自定义日期与双来源保存、详情刷新、原文件逐字节下载、生成入口 ID、错误字段保留、文件重选提示、浏览器大小限制、纯 DOCX 保存、错误班级拒绝、匿名下载拒绝。无 JavaScript 页面错误；已查看详情和错误页截图。
- Python 编译检查和 `git diff --check` 通过。项目虚拟环境未安装 Ruff，本次未运行 Ruff，未为此新增依赖。

边界与未验证风险：本次只在临时 SQLite 数据库及独立浏览器测试库验证，未直接迁移用户现有 `quiz_warehouse.db`；应用重启时会按现有机制执行新迁移。已有 PostgreSQL 库的 schema 升级未验证（项目当前 SQL runner 跳过非 SQLite 数据库）。未测试真实模型输出质量、进程强制终止/磁盘故障后的恢复或 OCR。413 总请求保护无法服务端回填未读取的正文；正常浏览器超大文件已在发送前阻止，表单内容保持原位。

工作区说明：继续使用 `teacher-copilot`，保留开始时已有修改；工作期间检测到这些原有班级流程改动由其他操作形成提交 `ccf934d`。本任务未执行提交或推送。

## 本次生成依据隔离（2026-09-07）

- [x] “使用已记录课程”只列出 URL 当前班级的课程日期和主题；选中后预览手动正文、原始文件名和文件提取摘要。从课程详情进入时通过 `lesson_id` 自动选中，伪造其他班级课程 ID 返回 404。
- [x] “输入本次内容”保留主题和内容/说明，至少填写一种；该方式不需要课程记录。切换来源会禁用另一组控件，服务端也只读取当前方式的字段，验证错误后保留可恢复的表单值。
- [x] Web 调用显式传入 `include_class_history=False`，上下文中的 `lesson_logs` 和 `assumed_knowledge` 置空。已记录课程方式只组合所选课程的主题、手动正文和文件提取文字；本次输入方式只使用本次主题和正文。Generator 与 Critic 接收相同的 `content_summary` 和空课程历史。
- [x] 生成元数据保存 `content_source.type`；课程方式另存课程 ID、日期、主题和安全化的原始文件 basename，本次输入方式保存主题及是否提供正文。测验详情用这些安全字段解释生成依据，不保存第二份正文或提取全文。
- [x] 默认题量改为 5。九种题型及默认选中的 Multiple Choice、True / False 保持不变；课程标准搜索/自定义代码、模型选择和难度控件保持原位置及功能。
- [x] 生成页、测验详情、题库、测验模板预览/导入、帮助、引导和评分标准生成说明不再向普通用户显示认知框架或认知层级。Web 生成忽略旧表单伪造的认知字段；后台参数、生成模块、导出兼容逻辑、历史测验字段和题目数据均保留。

本阶段没有增加字段或迁移；沿用课程流程已有的 014/015/016 迁移与 `lesson_logs` 文件字段。主要修改为 `src/quiz_generator.py`、`src/agents.py`、`src/web/blueprints/quizzes.py`、生成与结果相关模板，以及相应测试；没有改动课程标准数据、模型配置、题型实现、认知框架后台算法或 Critic 规则。

验证结果：

- 新增 14 项内容来源测试，覆盖当前班级选择与预览、跨班级拒绝、两种 Mock 生成、元数据、空输入、来源字段隔离、最近 14 天历史关闭、Generator/Critic 一致上下文、默认 5 题、旧认知数据兼容和普通页面隐藏。
- 课程、生成、认知后台兼容、班级、基础 Web、多班级端到端、安全、题库、模板、帮助和引导相关测试共 591 项通过。认知后台专项测试仍通过，说明历史字段与非 Web 通用调用未被删除。
- 本机 Edge 无头浏览器以真实登录和 CSRF 完成 12 项交互检查：从首页选择班级、班级锁定、默认题量、题型默认值、原有标准/模型/难度控件、认知区域隐藏、课程预览、两种 Mock 生成、详情入口自动选中、方式切换字段隔离和空输入错误。页面无 JavaScript 错误，并人工查看课程选择及错误状态截图。
- Python 编译检查和 `git diff --check` 通过。项目虚拟环境没有 Ruff，未为本任务增加依赖。

## 本次中文化与导出精简（2026-09-07）

- [x] 核心教师流程已使用简体中文：导航、登录、首页、班级详情、记录课程、课程详情、生成测验、测验详情、测验列表与题库均改用统一教育用语；页面声明 `lang="zh-CN"`。
- [x] 测验详情导出区只保留打印、教师版（含答案）的 Word 文档/PDF、学生版（不含答案）的 Word 文档/PDF。CSV、LMS、GIFT、QTI、Quizizz、模板导出和全局音频入口已从此页面移除；后台接口和逐题朗读保留。未确认测验仍在四个下载入口前显示确认提示。
- [x] “Question Bank” 统一显示为“题库”。题目按钮分别为“收藏到题库”和“已收藏”，悬停提示为“保存到题库”和“从题库移除”；成功、失败与移除操作均显示中文动态提示。题目操作区支持窄屏换行。
- [x] 新生成内容的 Agent 提示和 Mock 样例默认要求简体中文，同时保留外语教学目标、专有名词和原文引用的必要语言。既有课程和历史测验内容未批量改写。
- [x] PDF 导出通过捆绑的 Noto Sans SC（SIL OFL 1.1）嵌入中文字体；Word 导出设置 East Asian 字体为 Noto Sans SC，并使用 Microsoft YaHei 作为本机字体方案。课程、测验、评分标准、教案、学习材料和进度指南导出均复用该字体配置。

验证结果：课程、测验生成、教师确认和导出专项测试 113 项通过；`compileall` 与 `git diff --check` 通过。已在临时 SQLite 数据库和 Mock 模式下实际生成并查看教师版、学生版 PDF；中文长题干、答案解析、中英文混排、公式和分页未见方框、重叠或截断。DOCX 已生成并检查 East Asian 字体设置；本机缺少 LibreOffice，无法进行 Word 页面渲染。Edge 无头浏览器截图验证测验详情仅显示四个保留下载入口，题库按钮可见且窄屏操作区允许换行。

剩余风险：历史英文内容、第三方产品名与用户自行输入的英文保持原样；Word 未嵌入字体，在缺少 Noto Sans SC 或 Microsoft YaHei 的设备上排版可能略有差异。真实模型未调用，真实供应商的中文输出质量和延迟仍未验证。

剩余风险：未调用真实模型，因此未验证真实供应商对隔离后提示词的输出质量或成本；课程正文和文件提取预览各限制为 1000 字符，但生成仍使用完整内容；生成元数据只保存安全摘要，旧测验没有 `content_source` 时详情保持原有兼容显示。浏览器验证使用临时 SQLite 数据库，没有改动用户现有数据库。

## 本次生成状态与教师审核（2026-09-07）

- [x] 生成页不再用计时器宣称准备、生成或检查阶段完成；同步请求期间只显示中性等待信息。后端返回 `ProviderError` 时将其用户可读消息回填至原表单；未知服务端异常显示安全错误，教师可保留已填内容返回修改后重试。
- [x] `AgentMetrics` 记录唯一的 AI 检查来源：`ai_review_status` 和 `ai_review_reason`。状态覆盖 `passed/approved`、`not_passed/critic_rejected`、`not_passed/retry_limit`、`failed_or_incomplete/critic_error`、`failed_or_incomplete/insufficient_questions` 与 `failed_or_incomplete/generator_error`。Critic 调用异常时，结构有效的草稿可保存为 `needs_review`，但绝不会显示为检查通过。
- [x] SQLite 迁移沿用现有 runner，新增 017/018/019 三个最小字段：`quizzes.teacher_review_status`（默认 `pending_teacher_review`）、`teacher_confirmed_at`、`teacher_confirmed_by`。旧测验升级后保持待教师审核，绝不自动视为已确认。
- [x] 教师审核状态为“待教师审核”→“教师已确认”→“修改后需重新确认”→“教师已确认”。详情页的“确认可用”表单为带 CSRF token 的 POST；后端确认测验存在，记录 UTC 确认时间与已有会话中的显示名或用户名。当前已有的题目编辑（含答案）、删除、重新生成和图片内容修改均复用同一失效函数；只改标题不改变确认结果。
- [x] 导出端点的登录、权限和格式验证保持不变，也不会阻止未确认测验下载。详情页仅在未确认时显示醒目提示；点击任一导出链接会要求确认“该测验尚未经过教师确认，导出内容仍需人工核对”，确认后继续下载。已确认测验不显示该提示。

本阶段主要修改 `src/agents.py`、`src/database.py`、`src/migrations.py`、`src/quiz_generator.py`、`src/web/blueprints/quizzes.py`、测验生成/详情模板，以及新增 017/018/019 迁移与教师审核测试；课程标准、模型选择、题型、默认题型勾选和已隐藏的认知框架均未改动。

验证结果：

- Python 编译检查、`git diff --check` 以及教师审核、迁移、生成页状态、AI 状态、课程、内容来源、Web、生成、题目编辑和导出相关测试已运行。Mock 和临时 SQLite 环境中，476 项相关回归全部通过；其中教师审核、迁移、生成页状态和 Agent 状态专项 48 项通过。浏览器真实登录/CSRF 流程验证通过中性等待、Mock 生成详情跳转、未确认导出警告与继续、确认、编辑后失效和再次确认，且无 JavaScript 页面错误。
- 不调用真实付费模型。真实供应商的响应延迟、异常文案与输出质量仍未验证；本 MVP 仍使用同步请求，因此长时间模型调用只能呈现中性等待，不能提供真实分阶段进度。直接访问导出端点保持既有行为，界面警告不构成强制审批。

## 已有验证

## 测验生成页课程标准入口隐藏（2026-09-07）

- [x] `templates/quizzes/generate.html` 不再输出“课程标准（可选）”区块、标准搜索输入、搜索结果、已选标签、自定义代码说明或标准预览；该页面也不再加载 `standards_picker.js` 与 `standards_picker.css`，以及只服务于该选择器的预览、选择和删除代码。
- [x] Web 表单未提交 `sol_standards` 时，`quiz_generate` 会继续将班级已有课程标准传入生成器；直接调用方显式提交 `sol_standards` 时仍按其请求处理。课程标准管理、标准搜索 API、历史测验中的标准展示和其他页面的共享选择器均未删除。
- [x] `tests/test_standards_picker.py` 覆盖生成页无标准选择器、无资源引用和无辅助文案，以及未提交标准字段时沿用班级标准；相关标准管理 API 测试保留。

## 全站简体中文化与生成入口隐藏（2026-09-07）

- [x] 已将班级选择/列表/新建/编辑、课程记录列表、初始设置、引导、离线页、账户管理、模型服务设置、图片搜索、费用追踪、按主题生成、变体与评分标准直达页的教师界面转为简体中文；对应服务端 flash、表单错误、JSON 提示及浏览器弹窗同步使用中文。状态、阅读等级和题型的持久化值不变，仅在界面映射为中文。
- [x] 从 `templates/quizzes/detail.html` 移除了“生成变体”和“生成评分标准”两个普通用户入口，并一并移除空的 `section-actions` 容器；从变体空状态移除了指向生成变体地址的入口。既有变体数量、来源信息和评分标准列表仍显示，两个原有后端地址与其生成逻辑未删除。
- [x] 保留 QuizWeaver、LLM、AI、API、PDF、DOCX、Word、SQLite、SOL、Pixabay、OpenAI、Google Gemini、Vertex AI、Claude、Ollama、模型名、API 地址、课程标准代码和命令原文；用户输入、历史课程/测验正文及外部正式标题不做批量改写。

验证：`tests/test_web_variants.py` 已改为断言测验详情不含两个生成按钮或 URL，同时保留后端路由与已有数据展示断言。使用项目 `.venv` 运行定向测试、编译检查和 `git diff --check`；未提交或推送。

### 后续界面本地化补充

- 班级选择页的标题、说明和操作标签已改为中文；保存为英文的年级和学科值（例如 `7th Grade`、`Science`）通过展示层映射为“七年级”“科学”，不会改写历史数据或教师输入。
- 帮助与入门、学习表现分析、课程标准浏览／来源文件、课程计划、离堂测验和学习材料中的页面文案、表单、空状态、加载提示、确认框和直显 JSON 错误继续统一为简体中文；内部状态和 API 参数保持原值。

上轮在提交 `5faf02f` 上运行首页、导航、快捷键、课程价值说明、引导和 Web 基础测试，共 179 项通过。

本轮使用项目 `.venv`、MockLLMProvider 和临时数据库运行班级选择、首页、班级、课程记录、测验生成/编辑/导出、变体、评分标准、设置、响应式布局及保留模块相关测试，共 789 项通过。另以本机 Edge 无头浏览器完成首页入口、两个班级选择流程、非默认班级锁定、Mock 测验生成、课程记录、班级详情直达、非法目标拒绝和旧数据展示别名共 7 项交互检查，无 JavaScript 页面错误。未调用付费模型，也未验证真实供应商 API 或真实模型输出质量。
